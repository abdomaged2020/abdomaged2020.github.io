# تعليمات الموقع

## 1. بنية الملفات
```
index.html          -> الصفحة الرئيسية (البروفايل)
articles.html        -> صفحة مقالات التخطيط والجدولة
ai-planning.html      -> صفحة مقالات الذكاء الاصطناعي في التخطيط
css/styles.css       -> التصميم (الألوان، الخطوط، الأشكال)
js/                  -> منطق الموقع (تحميل البيانات، اللغة، الشات)
data/profile.json    -> كل بيانات البروفايل (الاسم، الخبرات، المهارات...)
data/articles-planning.json -> مقالات التخطيط
data/articles-ai.json       -> مقالات الذكاء الاصطناعي
data/faq.json        -> أسئلة وأجوبة الشات الذكي
assets/              -> الصورة الشخصية، الشعار، السيرة الذاتية PDF
```

**القاعدة الأساسية: كل تعديل على المحتوى (نص، خبرة، مقال، سؤال شات) بيتم في ملفات `data/*.json` فقط. مش محتاج تلمس أي كود HTML أو CSS أو JS.**

## 2. الحاجات اللي لازم تعدلها قبل النشر النهائي
افتح `data/profile.json` وابحث عن أي نص مكتوب جواه `[TODO: ...]` وابدله ببياناتك الحقيقية:
- نبذة عنك (summary)
- تواريخ الوظيفتين السابقتين (Motlaq Al-Ghweiry, Al-Khodary) وأهم مسؤولياتك فيهم
- رابط صفحتك على LinkedIn
- رقم الهاتف (اختياري - لو سبته فاضي مش هيظهر في الموقع)

## 3. إزاي تضيف مقال جديد
افتح `data/articles-planning.json` (أو `articles-ai.json` للذكاء الاصطناعي)، وأضف بلوك جديد بنفس الشكل ده في أول القائمة (لاحظ الفاصلة `,` بعد `}` القافلة):

```json
{
  "id": "معرف-فريد-للمقال",
  "title": { "ar": "عنوان المقال بالعربي", "en": "Article title in English" },
  "date": "2026-09-20",
  "summary": { "ar": "ملخص سطرين", "en": "Two-line summary" },
  "content": { "ar": "<p>محتوى المقال...</p>", "en": "<p>Article content...</p>" }
},
```

## 4. إزاي تضيف سؤال وجواب جديد للشات الذكي
افتح `data/faq.json` وأضف بلوك جديد:

```json
{
  "id": "معرف-فريد",
  "keywords": ["كلمة1", "كلمة2", "keyword-in-english"],
  "answer": { "ar": "الإجابة بالعربي", "en": "The answer in English" }
}
```
أي كلمة من `keywords` موجودة في سؤال الزائر، الشات هيرد بالإجابة دي تلقائيًا.

## 5. الصورة الشخصية والسيرة الذاتية
- استبدل `assets/avatar-placeholder.svg` بصورتك الحقيقية (احفظها بنفس الاسم، أو غيّر المسار `photo` في `profile.json`).
- ارفع ملف السيرة الذاتية PDF في `assets/` وحدّث حقل `cvFile` في `profile.json` بمساره (مثال: `"assets/CV.pdf"`)، وهيظهر زرار "تحميل السيرة الذاتية" تلقائيًا.

## 6. رفع الموقع على GitHub Pages
1. روح على الـ repository بتاعك: `abdomaged2020.github.io`
2. دوس "Add file" -> "Upload files"
3. اسحب كل الملفات والمجلدات (index.html, articles.html, ai-planning.html, css/, js/, data/, assets/) مرة واحدة
4. دوس "Commit changes"
5. استنى دقيقة أو اتنين، وافتح: `https://abdomaged2020.github.io`

لو الموقع مطلعش، تأكد من: Settings -> Pages -> إن الـ Source متظبط على "Deploy from a branch" والبranch هو main.

## 7. اختبار الموقع محليًا قبل الرفع (اختياري)
فتح `index.html` مباشرة بالدبل كليك ممكن ميوريش البيانات (لأن المتصفح بيمنع تحميل ملفات JSON محليًا لأسباب أمان). الموقع هيشتغل 100% بمجرد رفعه على GitHub Pages. لو عايز تجربه محليًا، شغّل من التيرمينال داخل مجلد الموقع:
```
python3 -m http.server 8000
```
وافتح `http://localhost:8000` في المتصفح.
